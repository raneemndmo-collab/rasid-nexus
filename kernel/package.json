{
  "name": "@rasid-nexus/kernel",
  "version": "0.0.1",
  "description": "RASID Nexus Kernel — Core services and interfaces",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
